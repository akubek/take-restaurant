/**
 * Data access layer for persistent domain objects.
 *
 * <p>This package contains Spring Data repositories responsible for CRUD operations
 * and custom query methods used by the service layer.
 *
 * <p>Repositories should remain focused on persistence concerns and avoid business orchestration.
 *
 * @since 1.0
 * @version 1.0
 */
package pl.polsl.take.restaurant.repository;
